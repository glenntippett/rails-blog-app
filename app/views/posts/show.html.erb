<% if @post.user == current_user %>
  <%= link_to "Delete Post",
              post_path(@post),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
<% end %>

<h1><%= @post.title %></h1>
<p>
  Written by: <%= @post.user.email %>
</p>
<%= @post.rich_body %>
